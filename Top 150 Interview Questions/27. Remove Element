class Solution:
    def removeElement(self, nums: List[int], val: int) -> int:
        print(val)
        for idx in range(len(nums)-1,-1,-1):
            if nums[idx] == val:
                del nums[idx]
                
        
